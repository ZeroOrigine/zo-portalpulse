PATCH INSTRUCTION for app/page.tsx (keeps hero, features, how-it-works, moments, FAQ, CTA; fixes truth + dedup + escape bugs):
1. Delete local `SiteHeader`, `SiteFooter`, `IconCheck`, `IconPlus`, `IconArrowRight`, `NAV_LINKS`, `PLANS`, `BTN_PRIMARY`, `BTN_SECONDARY` definitions. Add: `import { BTN_PRIMARY, BTN_SECONDARY, IconArrowRight, IconPlus, PlanGrid, SiteFooter, SiteHeader } from '@/components/marketing';` (IconMail/IconSparkles/IconShield/IconClock/IconFileCheck/IconLayers/IconArrowDown stay local — defined once).
2. Replace the entire pricing section's plan cards with `<PlanGrid />` (keep the section heading and the 'See full pricing details' link). This removes the phantom Enterprise plan and the wrong '3 GCs' free limit.
3. Copy corrections (truth): hero footnote and final-CTA footnote 'Free plan for up to 3 GCs.' -> 'Free for up to 2 GCs and 10 parsed emails a month.'; FAQ 'Is the free plan actually useful?' answer 'up to 3 GCs. Upgrade when you work with more GCs than that.' -> 'up to 2 GCs and 10 parsed portal emails a month. Upgrade to Pro when you outgrow either.'
4. Fix literal escape-sequence text bugs: replace every literal `\u00a9` with `&copy;`, `\u2019` with `&rsquo;`, `\u00b7` with the real middle-dot character, and `\u2026` with a real ellipsis character inside JSX text (they currently render as raw backslash text in the browser).